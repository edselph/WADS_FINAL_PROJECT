import React, {useState} from "react";
import axios from "axios";

const getDataAxios = async (search) => {
    const url = 'https://localhost:3000/furniture';
}
const searchBar = () => {
    const [products, setProducts] = useState([]);
}
